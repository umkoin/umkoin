#!/bin/bash
#
#  umkoind The bitcoin core server.
#
#
# chkconfig: 345 80 20
# description: umkoind
# processname: umkoind
#

# Source function library.
. /etc/init.d/functions

# you can override defaults in /etc/sysconfig/umkoind, see below
if [ -f /etc/sysconfig/umkoind ]; then
        . /etc/sysconfig/umkoind
fi

RETVAL=0

prog=umkoind
# you can override the lockfile via BITCOIND_LOCKFILE in /etc/sysconfig/umkoind
lockfile=${BITCOIND_LOCKFILE-/var/lock/subsys/umkoind}

# umkoind defaults to /usr/bin/umkoind, override with BITCOIND_BIN
umkoind=${BITCOIND_BIN-/usr/bin/umkoind}

# umkoind opts default to -disablewallet, override with BITCOIND_OPTS
umkoind_opts=${BITCOIND_OPTS--disablewallet}

start() {
    echo -n $"Starting $prog: "
    daemon $DAEMONOPTS $umkoind $umkoind_opts
    RETVAL=$?
    echo
    [ $RETVAL -eq 0 ] && touch $lockfile
    return $RETVAL
}

stop() {
    echo -n $"Stopping $prog: "
    killproc $prog
    RETVAL=$?
    echo
    [ $RETVAL -eq 0 ] && rm -f $lockfile
    return $RETVAL
}

case "$1" in
    start)
        start
        ;;
    stop)
        stop
        ;;
    status)
        status $prog
        ;;
    restart)
        stop
        start
        ;;
    *)
        echo "Usage: service $prog {start|stop|status|restart}"
        exit 1
        ;;
esac
